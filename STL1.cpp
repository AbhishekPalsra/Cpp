#include<bits/stdc++.h>
using namespace std;
int main()
{
 freopen("input.txt","r",stdin);
 freopen("output.txt","w",stdout);

   //data structure is a collection of data and arranged of data to utilixez it efficiently
   int arr[10]; // stack
   // one created array size cannot be changed
   // make bigger size array already
   // cant say exact space.. it can be wasted or limited/


   // here we can incrwase the size of array
    int *p=new int[10];  // heap
     p=new int[20]; // new araray



     // linked list can vary its size
     // single linked---> traverse in forward dirn only
     // doubly linked --> traverse in both dirn


     // stack
     // queue
     // dqueue
     //map
     // set










    return 0;
}