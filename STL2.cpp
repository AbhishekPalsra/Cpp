#include<bits/stdc++.h>
using namespace std;
int main()
{
 freopen("input.txt","r",stdin);
 freopen("output.txt","w",stdout);
   //containers are array link list stack,,,.. which storedata
   //function such as  search() sort( ) binary-seearch() reverse() concat() copy() union() intersection()m merge() heap()

   // container classes --> templated classes work for any type of data
   // vector -- variable size array
   // //  push_back() pop_back() insert() remove() size( empty() 
   // //  cannot insrt or delete from front
   // list -- doubly linked
   // // vector+ push_front() pop_front() front() back()
   // forward_list -- singly linked list 
   // //  push_front(), pop_front()
   // deque-- same as vector
   // // list   
   // priority_key --heap 
   // // push pop empty size 
   // stack lifo
   // // priority_key
   // set - only unique elements ,, duplicate are not allowed  ...no order
   // // insert()
   // multiset -- same as set but allow duplicate

   //map useful fir storing key value pair // use hashtable










    return 0;
}