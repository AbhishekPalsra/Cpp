#include<iostream>
#include<bits/stdc++.h>
using namespace std;  
int main()
{
    #ifndef ONLINE_JUDGE
      freopen("input.txt","r",stdin);
      freopen("output.txt","w",stdout);
      #endif
      // OOPS
      //  software is set of objects
      // objects----<set of fucntions >
      // to perform big diff tasks in same software
      //  data and function perform task on data
      // principle of oops 
      // 1 abstraction .. neednt to wonder how the function is working..internal details are hidden..only function name
      // 2. encapsultion : we make data as private and functions as public
      // 3. inheritnace - car--< toyota, swift,bmw> they inherit from the basic car
      // 4. Polymorphism  -- u neednt to learn all driving all 3 ccars..just need to learn upper hierachy learn car only.. all are derived from car only
      // class vs object
      // students belong to cse / ece class
      // students are the objects of cse/ece class
      // student class teacher class
      // human class
      // 






      return 0;



}