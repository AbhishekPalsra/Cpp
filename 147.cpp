#include<iostream>
#include<bits/stdc++.h>
using namespace std;  
int main()
{
    #ifndef ONLINE_JUDGE
      freopen("input.txt","r",stdin);
      freopen("output.txt","w",stdout);
      #endif
      // inline function machine code is not generated sepreately 
      // code is repalced in the same psoiton
    // whereas for other function seperate machine code is generated
    // writing inline before a function in a class makes it inline also           



      return 0;



}